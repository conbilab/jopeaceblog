# 복사해서 .env 로 저장하고 값을 채우세요:  cp .env.example .env
# (.env 는 절대 깃에 올리지 마세요)

# MinIO 관리자 계정 (아무 값이나 강하게)
MINIO_ROOT_USER=jopis-admin
MINIO_ROOT_PASSWORD=바꾸세요-아주-긴-랜덤-문자열

# Cloudflare Zero Trust → Networks → Tunnels 에서 터널 만들고 받은 토큰
CLOUDFLARE_TUNNEL_TOKEN=여기에-터널-토큰-붙여넣기
